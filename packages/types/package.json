{
  "name": "@chat-app/types",
  "version": "0.1.0",
  "private": true,
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint . --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
